class Ground {
  constructor() {
    var options = {
      isStatic: true
    }
    this.body = Bodies.rectangle(400,-1,800,1,options);
    this.width = width;
    this.height = height;
    World.add(world, this.body);
  }
  display(){}
};